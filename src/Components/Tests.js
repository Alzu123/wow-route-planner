import React from 'react'

const Tests = ({ destination }) => {
    return (
        <div>
            The fastest way to get to {destination} is by teleporting there lol.
        </div>
    )
}

export default Tests